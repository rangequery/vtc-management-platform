<form method="post" action="{{ path('app_demandeur_delete', {'id': demandeur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demandeur.id) }}">
    <button class="btn">Delete</button>
</form>
